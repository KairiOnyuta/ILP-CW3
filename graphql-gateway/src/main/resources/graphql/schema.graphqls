type Drone {
  name: String
  id: Int
  cooling: Boolean
  heating: Boolean
  capacity: Float
  maxMoves: Int
  costPerMove: Float
  costInitial: Float
  costFinal: Float
}

type Query {
  dronesWithCooling(state: Boolean!): [Drone!]!
  droneDetails(id: Int!): Drone
}
